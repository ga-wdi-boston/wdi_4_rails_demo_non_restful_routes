<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @app.name %>
</p>

<p>
  <b>Price:</b>
  <%= @app.price %>
</p>

<p>
  <b>Description:</b>
  <%= @app.description %>
</p>

<h2>Buy!</h2>
<%= form_for(@app, url: buy_app_path) do |f| %>
	<%= f.collection_select :transaction, @users, :id, :name %>
	<%= f.submit "Buy" %>
<% end %>


<%= link_to 'Back', apps_path %>
